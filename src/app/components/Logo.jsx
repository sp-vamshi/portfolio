
const Logo = ({className}) => {
    const logo = "<Vamshi/>"
  return (
    <div>
        <h1 className="font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-primary-400 to-secondary-600">{logo}</h1>
    </div>
  )
}

export default Logo